#define BOOST_AUTO_TEST_MAIN // Build main function
#include <boost/test/included/unit_test.hpp>

/* This macro generates a main function that runs all test cases automatically.
 *
 * When running the executable set the following command line parameters:
 * (In Eclipse: Run As / Run Configurations / Arguments)
 *
 * --build_info=yes --detect_memory_leak=1 --log_level=test_suite
 */
